const maxHeight=400;var ticking=false;function onScroll(){if(!ticking){requestAnimationFrame(realFunc);ticking=true}};function realFunc(){ticking=false};window.addEventListener('scroll',onScroll,false);$(document).on('click','.back-to-top',function(){$(this).addClass('to-top');window.scrollTo({top:0,behavior:'smooth'})});